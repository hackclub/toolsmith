{
  "name": "toolsmith",
  "version": "1.0.0",
  "description": "You ship a MCP tool, we ship AI credits",
  "license": "MIT",
  "author": "",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./main.css -o ./tw.css",
    "dev": "npx @tailwindcss/cli -i ./main.css -o ./tw.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.1",
    "tailwindcss": "^4.2.1"
  }
}
